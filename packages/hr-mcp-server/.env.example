# Env del proceso MCP RRHH (PM2 cuenti-time-mcp :4101)
# NO se empaqueta en deploy.tar.gz — créalo en el servidor.

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/faceaccess_saas"
MCP_HTTP_PORT=4101
MCP_HTTP_HOST=0.0.0.0
MCP_PUBLIC_URL="https://mcp-time.cuenti.co"
MCP_ALLOWED_HOSTS="mcp-time.cuenti.co,localhost,127.0.0.1"
MCP_JSON_RESPONSE=true
# Solo http local sin túnel:
# MCP_DANGEROUSLY_ALLOW_INSECURE_ISSUER_URL=true
